export function Hero() {
  return (
    <section className="py-12 px-4 text-center">
      <h1 className="text-3xl sm:text-4xl font-bold text-[var(--color-text)] max-w-3xl mx-auto">
        Every tool you need to work with audio in one place
      </h1>
      <p className="mt-4 text-lg text-[var(--color-text-muted)] max-w-2xl mx-auto">
        Merge, trim, convert, compress—all 100% FREE and easy to use. No login
        required.
      </p>
    </section>
  )
}
